version: '2'

services:
  elasticsearch:
    image: runelasticsearch
    volumes:
      #- ./elasticsearch/:/elasticsearch/
      - ./elasticsearch/:/elasticsearch/
      - ../../:/opt/
    networks:
      comm:
#        aliases:
#          - elasticsearch1

    restart: always

networks:
    comm:
