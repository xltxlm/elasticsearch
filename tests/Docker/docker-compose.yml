version: '2'

services:
  elasticsearch:
    image: runelasticsearch
    volumes:
      - ./elasticsearch/:/elasticsearch/
      - ../../:/opt/
    networks:
      comm:
        aliases:
          - elasticsearch
    ports:
        - "9200:9200/tcp"
    restart: always

networks:
    comm:
